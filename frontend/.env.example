# Base URL of the backend API. In local dev, Vite proxies /api to
# VITE_API_PROXY_TARGET (see vite.config.js), so this can usually stay as /api.
VITE_API_BASE_URL=/api

# Used only by vite.config.js's dev server proxy target.
VITE_API_PROXY_TARGET=http://localhost:4000
